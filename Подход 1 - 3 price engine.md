
# **Как симулировать цены без API (для хакатона или тестирования)**



Это очень полезно, потому что:

- API может не успеть подключиться
    
- могут быть лимиты
    
- можно быстро тестировать AI-логику
    
- демонстрация работает всегда
    

👉 В хакатонах часто используют именно mock-данные.


# 🟢 Подход 1 — Фейковый price engine (лучший вариант)

Ты создаёшь **модель расчёта цены**, которая выглядит как реальная динамическая система.

---

## ⭐ Простая модель цены

Цена зависит от:

- базовой цены отеля
    
- сезона
    
- дня недели
    
- спроса
    
- рейтинга
    
- района
    

---

### Пример формулы:

`price = base_price × season_multiplier × weekend_multiplier × demand_factor × hotel_rating_factor`

---

### Пример коэффициентов:

|Фактор|Значение|
|---|---|
|Базовая цена|$120|
|Высокий сезон|×1.5|
|Выходные|×1.2|
|Высокий рейтинг|×1.3|
|Низкий спрос|×0.8|

---

### Результат:

`120 × 1.5 × 1.2 × 1.3 = $280`

👉 Получается реалистично.

---

## Как задать сезонность

`Jan–Feb → low season → 0.8 Mar–May → medium → 1.0 Jun–Aug → high → 1.4 Sep–Oct → medium → 1.1 Nov–Dec → holiday → 1.6`

Можно адаптировать под город.

---

## Добавить шум (реализм)

Чтобы цены не выглядели искусственно:

`price = price + random(-10%, +10%)`

Это делает систему похожей на реальные OTA.

---

# 🟢 Подход 2 — Dataset отелей (очень просто)

Создаёшь JSON:

`{   "hotel_name": "Grand Plaza",   "base_price": 180,   "rating": 4.5,   "location_score": 9,   "district": "center" }`

Дальше price engine считает цену по формуле.

👉 Реализуется за 1–2 часа.

---

# 🟢 Подход 3 — Исторические цены (реалистичнее)

Создаёшь таблицу:

`hotel_id | month | avg_price`

И используешь:

`price = historical_avg × random_factor`

Можно сгенерировать автоматически.

----

# ⭐ Что рекомендую для твоего проекта

## Лучший вариант:

`JSON dataset + price engine + сезонность + random factor`

Почему:

- быстро
    
- реалистично
    
- контролируемо
    
- выглядит как production-логика

# 🚀 Как встроить в архитектуру

`User → вводит даты     ↓ Price Engine (mock)     ↓ возвращает цену     ↓ AI считает value score`

Позже можно заменить mock на реальный API — архитектура не меняется.